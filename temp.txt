You are working on the DocHubFE React + TypeScript frontend.

Goal: Update frontend pagination for workspace list and document list to match the new backend page-based paginated response format.

Backend now returns page-based pagination for:

```txt
GET /workspaces?page=1&limit=10
GET /workspaces/:workspaceId/documents?page=1&limit=10
```

Response format:

```ts
{
  data: T[]
  meta: {
    page: number
    limit: number
    totalItems: number
    totalPages: number
    hasNextPage: boolean
    hasPreviousPage: boolean
  }
}
```

Important UI rule:

* Workspace list UI has no filters.
* Document list UI has no filters.
* Workspace list UI does not need item-per-page selection.
* Document list UI does not need item-per-page selection.
* Always use a fixed default page size of `10`.
* Do not create any limit dropdown/select.
* Only manage `page` state in the UI.

Remove usage of:

* `cursor`
* `nextCursor`
* `hasMore`
* `loadMore`
* item-per-page selection UI

============================================================
PART 1 — Create shared pagination component
===========================================

Activity logs already have a pagination component:

```txt
src/features/activity/components/ActivityPagination.tsx
```

That component is generic enough to reuse because it only depends on:

```ts
meta.page
meta.totalPages
meta.hasNextPage
meta.hasPreviousPage
onPageChange
disabled
```

Create a shared component:

```txt
src/shared/components/Pagination.tsx
```

Suggested implementation:

```tsx
import { CaretLeft, CaretRight } from '@phosphor-icons/react'

export interface PagePaginatedMeta {
  page: number
  limit: number
  totalItems: number
  totalPages: number
  hasNextPage: boolean
  hasPreviousPage: boolean
}

interface PaginationProps {
  meta?: PagePaginatedMeta
  onPageChange: (page: number) => void
  disabled?: boolean
}

function getVisiblePages(page: number, totalPages: number): Array<number | '...'> {
  if (totalPages <= 5) {
    return Array.from({ length: totalPages }, (_, index) => index + 1)
  }

  if (page <= 3) {
    return [1, 2, 3, 4, '...', totalPages]
  }

  if (page >= totalPages - 2) {
    return [1, '...', totalPages - 3, totalPages - 2, totalPages - 1, totalPages]
  }

  return [1, '...', page - 1, page, page + 1, '...', totalPages]
}

export function Pagination({
  meta,
  onPageChange,
  disabled = false,
}: PaginationProps) {
  if (!meta || meta.totalPages <= 1) return null

  const pages = getVisiblePages(meta.page, meta.totalPages)

  return (
    <div className="mt-5 flex items-center justify-center gap-1">
      <button
        type="button"
        disabled={disabled || !meta.hasPreviousPage}
        onClick={() => onPageChange(meta.page - 1)}
        className="inline-flex h-8 w-8 items-center justify-center rounded-lg text-stone-500 transition hover:bg-stone-100 disabled:cursor-not-allowed disabled:opacity-40"
      >
        <CaretLeft size={16} />
      </button>

      {pages.map((pageItem, index) => {
        if (pageItem === '...') {
          return (
            <span
              key={`ellipsis-${index}`}
              className="inline-flex h-8 min-w-8 items-center justify-center px-2 text-sm font-medium text-stone-700"
            >
              ...
            </span>
          )
        }

        const active = pageItem === meta.page

        return (
          <button
            key={pageItem}
            type="button"
            disabled={disabled || active}
            onClick={() => onPageChange(pageItem)}
            className={`inline-flex h-8 min-w-8 items-center justify-center rounded-lg px-2 text-sm font-medium transition ${
              active
                ? 'border border-stone-200 bg-white text-stone-950 shadow-sm'
                : 'text-stone-900 hover:bg-stone-100'
            } disabled:cursor-default`}
          >
            {pageItem}
          </button>
        )
      })}

      <button
        type="button"
        disabled={disabled || !meta.hasNextPage}
        onClick={() => onPageChange(meta.page + 1)}
        className="inline-flex h-8 w-8 items-center justify-center rounded-lg text-stone-500 transition hover:bg-stone-100 disabled:cursor-not-allowed disabled:opacity-40"
      >
        <CaretRight size={16} />
      </button>
    </div>
  )
}
```

Then update Activity logs to use this shared component.

In:

```txt
src/pages/workspace/WorkspaceActivityPage.tsx
```

Replace:

```tsx
import { ActivityPagination } from '../../features/activity/components/ActivityPagination'
```

with:

```tsx
import { Pagination } from '../../shared/components/Pagination'
```

And replace:

```tsx
<ActivityPagination
  meta={logsQuery.data?.meta}
  disabled={logsQuery.isFetching}
  onPageChange={setPage}
/>
```

with:

```tsx
<Pagination
  meta={logsQuery.data?.meta}
  disabled={logsQuery.isFetching}
  onPageChange={setPage}
/>
```

Delete `ActivityPagination.tsx` only if no imports remain. Otherwise leave it temporarily.

============================================================
PART 2 — Create shared pagination types
=======================================

Create this file if it does not exist:

```txt
src/shared/types/pagination.type.ts
```

Content:

```ts
export interface PagePaginatedMeta {
  page: number
  limit: number
  totalItems: number
  totalPages: number
  hasNextPage: boolean
  hasPreviousPage: boolean
}

export interface PagePaginatedResponse<T> {
  data: T[]
  meta: PagePaginatedMeta
}
```

Use this shared type for workspace and document list responses.

Do not over-refactor activity types unless it is simple and safe.

============================================================
PART 3 — Update workspace frontend pagination
=============================================

Files to inspect and update:

```txt
src/features/workspaces/types/workspace.type.ts
src/features/workspaces/services/workspace.service.ts
src/features/workspaces/hooks/useWorkspace.ts
src/pages/workspace-list/WorkspaceListPage.tsx
```

Current problem:

* Workspace FE still uses cursor pagination:

  * `nextCursor`
  * `hasMore`
  * `loadMore`
* Backend now uses page-based pagination.

Required behavior:

* Keep only `page` state.
* Always request `limit: 10`.
* Do not create limit selection UI.
* Do not use load more.

Use this constant:

```ts
const WORKSPACE_PAGE_LIMIT = 10
```

1. Update workspace response type

In `workspace.type.ts`, remove or stop using cursor pagination shape:

```ts
nextCursor: string | null
hasMore: boolean
```

Use:

```ts
import type { PagePaginatedResponse } from '../../../shared/types/pagination.type'

export type WorkspaceListResponse = PagePaginatedResponse<Workspace>
```

Or update the existing `PaginatedResponse<T>` to page-based if that is less invasive.

2. Update workspace service

Change:

```ts
getWorkspaces(params?: { cursor?: string; limit?: number })
```

to:

```ts
getWorkspaces(params?: { page?: number; limit?: number })
```

Implementation concept:

```ts
getWorkspaces(params?: { page?: number; limit?: number }) {
  return api
    .get<PagePaginatedResponse<Workspace>>('/workspaces', {
      params,
    })
    .then((res) => res.data)
}
```

Do not send `cursor`.

3. Update `useWorkspaces`

Replace cursor/load-more logic with page-based logic.

Suggested hook:

```ts
import { useEffect, useState } from 'react'
import { workspaceService } from '../services/workspace.service'
import type { Workspace } from '../types/workspace.type'
import type { PagePaginatedMeta } from '../../../shared/types/pagination.type'

const WORKSPACE_PAGE_LIMIT = 10

export function useWorkspaces(page = 1) {
  const [workspaces, setWorkspaces] = useState<Workspace[]>([])
  const [meta, setMeta] = useState<PagePaginatedMeta | undefined>()
  const [isLoading, setIsLoading] = useState(true)
  const [isFetching, setIsFetching] = useState(false)
  const [error, setError] = useState<string | null>(null)

  const fetchWorkspaces = async () => {
    setIsFetching(true)
    setError(null)

    try {
      const result = await workspaceService.getWorkspaces({
        page,
        limit: WORKSPACE_PAGE_LIMIT,
      })

      setWorkspaces(result.data)
      setMeta(result.meta)
    } catch {
      setError('Could not load workspaces.')
    } finally {
      setIsLoading(false)
      setIsFetching(false)
    }
  }

  useEffect(() => {
    fetchWorkspaces()
  }, [page])

  return {
    workspaces,
    meta,
    isLoading,
    isFetching,
    error,
    refetch: fetchWorkspaces,
  }
}
```

Important:

* Do not expose `limit` from the hook.
* Do not expose `loadMore`.
* Do not expose `nextCursor`.
* Do not expose `hasMore`.

4. Update `WorkspaceListPage`

Add page state:

```tsx
const [page, setPage] = useState(1)
```

Use:

```tsx
const {
  workspaces,
  meta,
  isLoading,
  isFetching,
  error,
} = useWorkspaces(page)
```

Render shared pagination under `WorkspaceGrid`:

```tsx
<Pagination
  meta={meta}
  disabled={isFetching}
  onPageChange={setPage}
/>
```

Make sure to import:

```tsx
import { Pagination } from '../../shared/components/Pagination'
```

============================================================
PART 4 — Update documents frontend pagination
=============================================

Files to inspect and update:

```txt
src/features/documents/types/document.type.ts
src/features/documents/service/document.service.ts
src/features/documents/hooks/useDocuments.ts
src/pages/workspace/WorkspaceDocumentsPage.tsx
```

File names may vary slightly. Inspect the project and update the actual matching files.

Required behavior:

* Keep only `page` state.
* Always request `limit: 10`.
* Do not create limit selection UI.
* Do not use load more.
* Do not use cursor pagination.

Use this constant:

```ts
const DOCUMENT_PAGE_LIMIT = 10
```

1. Update document service

Change `getDocuments` to accept page params:

```ts
getDocuments(
  workspaceId: string,
  params?: {
    page?: number
    limit?: number
  },
)
```

Expected return type:

```ts
PagePaginatedResponse<Document>
```

Implementation concept:

```ts
async getDocuments(
  workspaceId: string,
  params?: { page?: number; limit?: number },
): Promise<PagePaginatedResponse<Document>> {
  const res = await api.get<PagePaginatedResponse<Document>>(
    `/workspaces/${workspaceId}/documents`,
    { params },
  )

  return res.data
}
```

Make sure the endpoint path matches the existing service path style.

2. Update `useDocuments`

Use normal `useQuery`, not `useInfiniteQuery`.

Suggested hook:

```ts
import { useQuery } from '@tanstack/react-query'
import { documentService } from '../service/document.service'

const DOCUMENT_PAGE_LIMIT = 10

export function useDocuments(
  workspaceId?: string,
  page = 1,
) {
  return useQuery({
    queryKey: ['documents', workspaceId, page],
    queryFn: () =>
      documentService.getDocuments(workspaceId!, {
        page,
        limit: DOCUMENT_PAGE_LIMIT,
      }),
    enabled: Boolean(workspaceId),
    placeholderData: (previousData) => previousData,
  })
}
```

Important:

* Do not expose `limit` from the hook.
* The query result data is now:

  ```ts
  {
    data: Document[]
    meta: PagePaginatedMeta
  }
  ```
* It is not `Document[]` anymore.

3. Update document mutations invalidation if needed

Existing mutations probably invalidate:

```ts
queryClient.invalidateQueries({
  queryKey: ['documents', workspaceId],
})
```

This should still work if the query key starts with `['documents', workspaceId]`.

If invalidation is too exact and only targets the old query key, update it to prefix invalidation:

```ts
queryClient.invalidateQueries({
  queryKey: ['documents', workspaceId],
})
```

4. Update `WorkspaceDocumentsPage`

Add page state:

```tsx
const [page, setPage] = useState(1)
```

Use:

```tsx
const documentsQuery = useDocuments(workspaceId, page)

const documents = documentsQuery.data?.data ?? []
const meta = documentsQuery.data?.meta
```

Render pagination under the documents table/grid:

```tsx
<Pagination
  meta={meta}
  disabled={documentsQuery.isFetching}
  onPageChange={setPage}
/>
```

Make sure existing rendering still works:

```tsx
<DocumentTable
  workspaceId={workspaceId}
  documents={documents}
/>
```

or whatever the current component expects.

Preserve:

* existing document empty state
* existing document create/upload modal behavior
* existing document delete/rename/share behavior
* existing loading and error UI

============================================================
PART 5 — Do not change
======================

Do not modify:

* Backend code
* API endpoint names
* Auth logic
* Permission utils
* Document table/card UI except for passing paginated data
* Activity log behavior except moving pagination component to shared
* Apryse/PDF viewer
* Upload progress logic
* Share modal logic

Do not add:

* filter UI
* page-size selector
* item-per-page selector
* load-more button

============================================================
PART 6 — Quality requirements
=============================

After changes:

* TypeScript should compile.
* No unused imports.
* No references to workspace `nextCursor`, `hasMore`, or `loadMore`.
* No document list code should expect `Document[]` directly from `useDocuments`.
* Workspace list should use numbered pagination with fixed `limit = 10`.
* Documents list should use numbered pagination with fixed `limit = 10`.
* Activity logs should still work with the shared `Pagination` component.
* Pagination UI should be visually consistent across:

  * Activity logs
  * Workspace list
  * Documents list

Please implement frontend changes only.
