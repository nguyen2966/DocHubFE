Refactor FE shared document detail without touching existing normal document hooks.

Constraints:
- Do not modify existing normal document hooks such as useDocumentDetail, useEditPdf, useDocuments, useCreateMarkdownDocument, upload hooks, or comment hooks.
- It is okay to update shared-with-me specific service/hook/types because BE now returns paginated shared documents and shared detail returns permissions.
- Do not change backend API routes.

Tasks:

1. Create reusable component:
   src/features/documents/components/details-page/DocumentDetailExperience.tsx

   Move the full document interaction experience from:
   src/pages/document-details/DocumentDetailPage.tsx

   into DocumentDetailExperience.

   Move:
   - comment popover/sidebar state
   - Apryse viewer refs
   - PDF edit state
   - comment create/reply/edit/delete handlers
   - pending comment composer
   - hover preview
   - delete modals
   - layout with Header, toolbar, title bar, viewer shell, comment panel

   Do not move useDocumentDetail into this component.
   DocumentDetailExperience receives already-fetched document data.

   Props:
   interface DocumentDetailExperienceProps {
     workspaceId: string
     documentId: string
     document: Document
     backElement: React.ReactNode
   }

   Replace DocumentBackButton inside the moved JSX with {backElement}.

2. Simplify WorkspaceDocumentDetailPage:
   - Keep useParams and useDocumentDetail here.
   - Keep loading/error/404 handling here.
   - Render DocumentDetailExperience after document is loaded.
   - Pass:
       workspaceId
       documentId
       document
       backElement={<DocumentBackButton workspaceId={workspaceId} />}

3. Simplify SharedDocumentDetailPage:
   File:
   src/pages/share-with-me-details/SharedDocumentDetail.tsx

   - Remove direct usage of DocumentViewerToolbar, DocumentTitleBar, and DocumentViewerShell.
   - Keep useSharedDocumentDetail here.
   - Keep loading/error states here.
   - Render DocumentDetailExperience after document is loaded.
   - Pass:
       workspaceId={document.workspaceId}
       documentId={documentId}
       document={document}
       backElement = a button that navigates to /shared-with-me

   The shared detail page should reuse the exact same edit/comment/view logic as normal document detail.

4. Adapt shared-with-me list to paginated response.

   In document.type.ts:
   - Add permissions?: DocumentPermission[] to SharedDocument.
   - Add:
     export type SharedDocumentListResponse = PagePaginatedResponse<SharedDocument>

   In document.service.ts:
   - Change only getSharedWithMeDocuments to accept params { page?: number; limit?: number }.
   - Return Promise<SharedDocumentListResponse>.
   - Call GET /shared-with-me/documents with params.

   In userSharedDocuments.ts:
   - Change useSharedDocuments to accept params { page?: number; limit?: number }.
   - Use queryKey ['shared-with-me', 'documents', params].
   - Query with documentService.getSharedWithMeDocuments(params).

5. Update ShareWithMePage:
   File:
   src/pages/share-with-me/ShareWithMePage.tsx

   - Add local page state.
   - Call useSharedDocuments({ page, limit: 12 }).
   - Use:
       const documents = data?.data ?? []
       const meta = data?.meta
   - Use Total {meta?.totalItems ?? 0}, not documents.length.
   - Import Pagination from src/shared/components/Pagination.
   - Render Pagination below SharedDocumentTable:
       <Pagination meta={meta} disabled={isFetching} onPageChange={setPage} />

6. Preserve existing UI behavior:
   - Workspace document detail should look and behave the same.
   - Shared document detail should now support the same viewer/comment/edit experience based on document.permissions.
   - Viewer users should only view.
   - Commenter users should view/comment.
   - Editor users should view/comment/edit.
   - Shared users should not see actions unless document.permissions allows them.

Acceptance criteria:
- No duplicated full document detail implementation.
- WorkspaceDocumentDetailPage is only a data wrapper.
- SharedDocumentDetailPage is only a data wrapper.
- DocumentDetailExperience is the single source for document detail UI/interaction.
- ShareWithMePage supports paginated data and shared Pagination component.
- Existing normal document hooks are not modified.
- App builds successfully with npm run build.